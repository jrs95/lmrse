% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lmrse.R
\name{robustse}
\alias{robustse}
\title{Robust SE}
\usage{
robustse(y = y, x = x, cluster = cluster)
}
\arguments{
\item{y}{matrix of methylation.}

\item{x}{matrix of other covariates (including intercept).}

\item{cluster}{clustering variable.}
}
\value{
A matrix of robust standard errors where the rows are the markers (e.g. CpG sites of DNA methylation) and the columns are the covariates including the intercept.
}
\description{
robustse fits cluster robust standard errors (with robusteCpp and/or robustseR) for longitudinal analyses across markers using standard linear regression.
}
\examples{
### Data
y <- rnorm(5000000)
y <- matrix(y, ncol=1000)
colnames(y) <- paste0("var",1:1000)
x <- rnorm(5000)
cluster <- rep(1:1000,5)
covar <- cbind(rbinom(5000,1,0.5),rnorm(5000))
X <- cbind(1,x,covar)
colnames(X) <- c("intercept", "X", "covar1", "covar2")

### Analyses
beta <- t((solve(crossprod(X))\%*\%t(X))\%*\%y)
se <- robustse(y=y, x=X, cluster=cluster)
}
\author{
James R Staley <js16174@bristol.ac.uk>
}
